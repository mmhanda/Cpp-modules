#include <vector>
#include <deque>
#include <iostream>
#include <sys/time.h>
#include <iomanip>

void merge_sort_v(std::vector<int> &numbers_d);
std::deque<int> merge_sort_d(std::deque<int> numbers_d);